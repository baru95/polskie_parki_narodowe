import 'package:flutter/material.dart';

class CuriositiesPanel extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Aktualności'),
        actions: <Widget>[],
      ),
      body: Text('aaa'),
    );
  }
}